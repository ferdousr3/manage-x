# Environment
NODE_ENV=development
PORT=3000

# Database (Neon PostgreSQL or local)
DATABASE_URL=postgresql://user:password@localhost:5432/manage_x
DB_MODE=pg

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Backend URL
BACKEND_URL=http://localhost:3000

# JWT Secrets (generate strong secrets for production)
ACCESS_TOKEN_SECRET=your-access-token-secret-min-32-chars
REFRESH_TOKEN_SECRET=your-refresh-token-secret-min-32-chars

# Email (Resend)
EMAIL_RESEND_API_KEY=re_xxxxxxxxxxxxx
EMAIL_SENDER_NAME=Manage-X
EMAIL_SENDER_EMAIL=noreply@yourdomain.com
